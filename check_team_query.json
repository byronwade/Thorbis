{"query": "SELECT tm.id, tm.user_id, tm.company_id, tm.status, tm.role_id, u.email, u.name, c.name as company_name FROM team_members tm LEFT JOIN users u ON tm.user_id = u.id LEFT JOIN companies c ON tm.company_id = c.id ORDER BY tm.created_at DESC LIMIT 10"}
