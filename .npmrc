# Hoist dependencies to make them available for Next.js/Turbopack
# This ensures workspace package dependencies are resolvable
public-hoist-pattern[]=*clsx*
public-hoist-pattern[]=*tailwind-merge*
public-hoist-pattern[]=*@stratos/*

# Shamefully hoist all dependencies for Next.js compatibility
# This is needed because Next.js/Turbopack has issues with pnpm's strict isolation
shamefully-hoist=true




